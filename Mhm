Old = hookfunction(game.FindFirstChild,function(...)
    if pcall(function() x = getcallingscript().Parent end) then
        warn('ass'..tostring(getcallingscript().Parent))
        return nil
    end
    return Old(...)
end)
