Old = hookfunction(game.FindFirstChild,function(...)
    if pcall(function() x = getcallingscript().Parent end) and getcallingscript().Parent == game.Players.LocalPlayer.PlayerGui.Menew then
        return nil
    end
    return Old(...)
end)
